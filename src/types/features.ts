export type FeatureConfig = {
  menu: boolean;
  pageTurnMenu: boolean;

  gallery: boolean;
  galleryLightbox: boolean;

  reservations: boolean;

  chefSection: boolean;
  privateDining: boolean;
  events: boolean;

  newsletter: boolean;

  smoothScroll: boolean;
  analytics: boolean;
};